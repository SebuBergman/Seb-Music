import { SectionTitle } from "components/UI/Typography/Typography";
import { Wrapper } from "./styled";

function Error() {
  return (
    <Wrapper>
      <SectionTitle>Page was not found</SectionTitle>
    </Wrapper>
  );
}

export default Error;
